ktor {
    deployment {
        port = 8080
        host = "0.0.0.0"
    }
    application {
        modules = [ com.stslex.ApplicationKt.module ]
    }
}

postgres {
    url = "jdbc:h2:mem:test"
    user = "user"
    password = "password"
}

apiKey = "test-key"

jwt {
    auth {
        secret = "auth-secret"
    }
    unAuth {
        secret = "refresh-secret"
    }
}
